/** @type {import('next').NextConfig} */
const nextConfig = {
    experimentlal:{
        appDir: true,
    },
    images:{
        domains:[
            "avatars.githubusercontent.com"
        ]
    }
};

module.exports = nextConfig
